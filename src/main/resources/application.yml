---
server:
    port: 8081
spring:
    application:
      name: account
    profiles:
      active: prod
    config:
      import: optional:configserver:http://configserver:8070
management:
  auditevents:
    enabled: true
  endpoints:
    web:
      exposure:
        include: '*'